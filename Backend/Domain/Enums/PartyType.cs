namespace Domain.Enums;

/// <summary>
/// Определяет сторону доменных действий, доступных юристу или клиенту.
/// Используется в контекстах комментариев, подписей и запросов на удаление.
/// </summary>
public enum PartyType
{
    /// <summary>Действие выполняется юристом.</summary>
    Lawyer,

    /// <summary>Действие выполняется клиентом.</summary>
    Client
}
